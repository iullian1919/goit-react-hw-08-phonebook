import React from 'react';
import { Center, Text } from '@chakra-ui/react';

const HomePage = () => {
  return (
    <Center paddingTop={30}>
      <Text fontSize="24px">
        You need to register before using the service.
      </Text>
    </Center>
  );
};

export default HomePage;
